<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inneed.backend.mapper.RoleTableMapper">
    <select id="getRightsByRoleId" resultType="string">
        select rights from role_table
        where id = #{id}
    </select>
</mapper>